name: Scrape Leilões RJ

on:
  schedule:
    - cron: '0 9 * * 1'
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - run: npm ci

      - name: Scrape Leilão Imóvel (Apify)
        run: node scraper/apify-leilaoimovel.js
        env:
          APIFY_TOKEN: ${{ secrets.APIFY_TOKEN }}
          MAX_ITEMS: '200'
